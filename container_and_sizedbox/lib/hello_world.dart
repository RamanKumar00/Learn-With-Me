import 'package:flutter/material.dart';

class printword extends StatelessWidget {
  const printword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rahul")),
      body: Center(child:
       Container(child:
        Text("Hello World")
        )
        ),
    );
  }
}
